"use strict";
exports.__esModule = true;
var curryFn = function (fn, argsCache, self) {
    return fn.length <= argsCache.length
        ? fn.apply(self, argsCache.slice(0, fn.length))
        : function () {
            var ys = [];
            for (var _i = 0; _i < arguments.length; _i++) {
                ys[_i] = arguments[_i];
            }
            return curryFn(fn, argsCache.concat(ys), self);
        };
};
exports.curry = function (fn, self) { return curryFn(fn, [], self); };
